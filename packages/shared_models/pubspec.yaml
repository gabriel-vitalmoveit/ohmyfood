name: shared_models
description: Shared DTOs and domain models for OhMyFood
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  collection: ^1.18.0

dev_dependencies:
  test: ^1.24.0
